# Plan 15-01: Install Skills + Wire Humanization Gate

**Phase:** 15-humanization-gate
**Plan:** 01
**Status:** Complete

## Goal

Make AI-written scripts and publish metadata read as human-written by integrating the `humanize` and `ai-check` skills as a mandatory, bounded quality gate in the Script and Publish stages.

## Tasks

1. Vendor `humanize` + `ai-check` skills into `.agents/skills/` with MIT license attribution
2. Symlink skills into `.claude/skills/` and `.opencode/skills/` (existing per-skill pattern)
3. Add Phase E.5 Humanization Gate to `viral-script.md` (1 ai-check, max 2 humanize passes, no re-check)
4. Wire gate into longform/shortform/LinkedIn confirmation + persistence points
5. Add metadata humanization step to both `publish-video.md` variants
6. Update AGENTS.md, `.agents/docs/script.md`, `.agents/docs/publish.md`, viral-pipeline agent prompt
7. Allow `humanize`/`ai-check` in `opencode.json` for build + viral-pipeline agents
8. Wire voice matching: `channels/{name}/voice/` convention + init scaffold + README
9. Add humanize skill + voice sample checks to `scripts/doctor.py` and `viral-setup.md`
10. Verify: 734 tests pass, opencode.json valid, symlinks resolve, doctor PASS

## Verification Loop

- `python3 -m pytest tests/ -q` → 734 passed, 2 skipped
- `python3 -c "import json; json.load(open('.opencode/opencode.json'))"` → valid
- `ls -la .claude/skills/{humanize,ai-check} .opencode/skills/{humanize,ai-check}` → symlinks resolve
- `python3 scripts/doctor.py` → `Humanize skills installed [PASS]`, `Voice samples (ChannelA) [PASS]`

## Requirement

HUMANIZE-01 through HUMANIZE-05