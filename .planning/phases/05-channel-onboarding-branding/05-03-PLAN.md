---
phase: 05-channel-onboarding-branding
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified:
  - schemas/channel-config.schema.json
  - scripts/setup-channel-branding.py
  - .agents/commands/viral-onboard.md
autonomous: true
requirements: [CHANNEL-06, CHANNEL-07]

must_haves:
  truths:
    - "Default upload settings (privacy, license, embed, comments) are persisted to channel_config.json and survive script re-runs"
    - "channel_config.json is validated against the schema before saving — invalid configs are rejected with a clear error"
    - "The agent onboarding command documents the manual avatar limitation and the refactored CLI usage"
  artifacts:
    - path: "schemas/channel-config.schema.json"
      provides: "Schema definition including `branding` and `defaults` fields"
      contains: "properties.branding, properties.defaults"
    - path: "scripts/setup-channel-branding.py"
      provides: "Schema validation before save, default upload settings flags"
      contains: "validate_or_raise, --default-privacy, --default-license, --allow-embed, --allow-comments"
    - path: ".agents/commands/viral-onboard.md"
      provides: "Updated agent onboarding command with manual avatar note and refactored CLI usage"
      contains: "manual avatar, setup-channel-branding.py usage"
  key_links:
    - from: "scripts/setup-channel-branding.py"
      to: "agent_core/core/validation.py"
      via: "validate_or_raise import"
      pattern: "from agent_core.core.validation import validate_or_raise"
    - from: "scripts/setup-channel-branding.py"
      to: "schemas/channel-config.schema.json"
      via: "schema validation call"
      pattern: "validate_or_raise.*channel-config"
    - from: "scripts/setup-channel-branding.py"
      to: "channels/{Name}/channel_config.json"
      via: "json.dumps save"
      pattern: "channel_config.json"
---

<objective>
Add schema validation to the channel branding save flow and update the channel config schema to include `branding` and `defaults` fields, completing the persistence integration for CHANNEL-06 and CHANNEL-07.

Purpose: Ensure all channel branding data (description, keywords, avatar/banner/watermark paths, default upload settings) is persisted to `channels/{Name}/channel_config.json` with schema validation before saving, so Phase 6 (publishing) can read defaults reliably.

Output:
- Updated `schemas/channel-config.schema.json` with `branding` and `defaults` fields
- Updated `scripts/setup-channel-branding.py` with schema validation before save
- Updated `.agents/commands/viral-onboard.md` with manual avatar note and refactored CLI usage
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-channel-onboarding-branding/05-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md

<interfaces>
From agent_core/core/validation.py:
```python
def validate_or_raise(data: Any, schema_name: str) -> Any:
    """Validate and return data, or raise ValueError on failure."""
    result = validate_output(data, schema_name)
    if not result.success:
        raise ValueError(f"Schema validation failed for {schema_name}: {'; '.join(result.errors)}")
    return data
```

From schemas/channel-config.schema.json (current):
- `required: ["channel_name", "brand", "youtube", "production"]`
- `brand` object with: name, tagline, primary_color, secondary_color, font_primary, font_display, logo_svg
- `youtube` object with: channel_id, oauth_token_path, upload_defaults (category, language, visibility)
- `production` object with: render_width, render_height, fps, tts

From scripts/setup-channel-branding.py (current state):
- Already has `--default-privacy`, `--default-license`, `--allow-embed`, `--allow-comments` flags
- Already persists to `config["defaults"]` and `config["branding"]`
- Missing: schema validation before save
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update channel-config.schema.json with branding and defaults fields</name>
  <files>schemas/channel-config.schema.json</files>
  <action>
    Add two new optional properties to `schemas/channel-config.schema.json`:

    1. `branding` (object, not required) — YouTube channel branding data set by `setup-channel-branding.py`:
       - `description` (string) — channel description
       - `keywords` (string) — comma-separated channel tags
       - `country` (string) — ISO 3166-1 alpha-2 country code
       - `default_language` (string) — BCP-47 language tag (e.g., "en")
       - `avatar_path` (string) — path to channel avatar image
       - `banner_path` (string) — path to channel banner image
       - `watermark_path` (string) — path to watermark image
       - `channel_id` (string) — YouTube channel ID

    2. `defaults` (object, not required) — per-upload default settings tracked locally (YouTube API has no endpoint for channel-level defaults):
       - `privacy` (string, enum: ["public", "unlisted", "private"])
       - `license` (string, enum: ["youtube", "creativeCommon"])
       - `embed` (boolean)
       - `comments` (boolean)

    Do NOT change the existing `required` array — `branding` and `defaults` are optional. Do NOT modify existing `brand`, `youtube`, or `production` properties.
  </action>
  <verify>
    <automated>python3 -c "import json; s=json.load(open('schemas/channel-config.schema.json')); assert 'branding' in s['properties']; assert 'defaults' in s['properties']; assert s['properties']['branding']['type']=='object'; assert s['properties']['defaults']['type']=='object'; print('Schema OK')"</automated>
  </verify>
  <done>Schema has `branding` (description, keywords, country, default_language, avatar_path, banner_path, watermark_path, channel_id) and `defaults` (privacy, license, embed, comments) as optional object properties. Existing `brand`, `youtube`, `production` properties and `required` array are unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add schema validation to setup-channel-branding.py before save</name>
  <files>scripts/setup-channel-branding.py</files>
  <action>
    Add schema validation to `scripts/setup-channel-branding.py` before the `save_channel_config()` call:

    1. Add import at top of file (after existing imports, before the `SCRIPT_DIR` line):
       ```python
       from agent_core.core.validation import validate_or_raise
       ```

    2. Add `sys.path.insert(0, str(PROJECT_ROOT))` after `PROJECT_ROOT = SCRIPT_DIR.parent` to ensure the `agent_core` package is importable when running as a script.

    3. Before the `save_channel_config(args.channel, config)` call on line 208, add schema validation:
       ```python
       # Validate against schema before saving
       try:
           validate_or_raise(config, "channel-config.schema.json")
       except ValueError as e:
           print(f"  ERROR: Config validation failed: {e}")
           print("  Config was NOT saved. Fix the issues above and re-run.")
           sys.exit(1)
       ```

    Per D-11 (error messages include actionable next command), the error message tells the user what to fix and re-run.

    Do NOT change any existing flag handling, API calls, or persistence logic. The `--default-privacy`, `--default-license`, `--allow-embed`, `--allow-comments` flags and their persistence to `config["defaults"]` already exist and work correctly — only add the validation gate before save.
  </action>
  <verify>
    <automated>python3 -c "import ast; tree=ast.parse(open('scripts/setup-channel-branding.py').read()); assert any(isinstance(n, ast.ImportFrom) and n.module and 'validation' in n.module for n in ast.walk(tree)); print('validate_or_raise import found')"</automated>
  </verify>
  <done>setup-channel-branding.py imports `validate_or_raise` from `agent_core.core.validation` and calls it before `save_channel_config()`. Invalid configs raise ValueError with a clear message and exit code 1. Valid configs save normally.</done>
</task>

<task type="auto">
  <name>Task 3: Update viral-onboard.md with manual avatar note and refactored CLI usage</name>
  <files>.agents/commands/viral-onboard.md</files>
  <action>
    Update `.agents/commands/viral-onboard.md` with two changes:

    1. Add a note about the manual avatar limitation (per D-05, D-07):
       - After the branding step, add: "NOTE: YouTube API has no endpoint for channel profile picture (avatar). The user must upload the avatar manually via YouTube Studio (https://studio.youtube.com/channel/{channel_id}/branding). The avatar_path is stored in channel_config.json for documentation only."

    2. Update the `setup-channel-branding.py` usage line to reflect the full CLI including default settings flags:
       ```
       python scripts/setup-channel-branding.py --channel {name} --description "..." --keywords "tag1,tag2" [--default-privacy private|unlisted|public] [--default-license youtube|creativeCommon] [--allow-embed true|false] [--allow-comments true|false]
       ```

    The current file has 13 lines. Replace the entire content with the updated version preserving all existing content and adding the new information.
  </action>
  <verify>
    <automated>grep -q "manual" .agents/commands/viral-onboard.md && grep -q "default-privacy" .agents/commands/viral-onboard.md && echo "viral-onboard.md updated OK"</automated>
  </verify>
  <done>viral-onboard.md documents the manual avatar limitation (per D-05, D-07) and shows the full CLI usage including default settings flags.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script → filesystem | `setup-channel-branding.py` writes to `channels/{Name}/channel_config.json` |
| script → schema file | `setup-channel-branding.py` reads `schemas/channel-config.schema.json` for validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | T (Tampering) | `channel_config.json` write | mitigate | Schema validation via `validate_or_raise` rejects malformed configs before write; ValueError with clear message prevents silent corruption |
| T-05-03-02 | I (Info Disclosure) | `channel_config.json` | accept | Config stores only channel metadata (description, paths) and upload defaults — no secrets. OAuth tokens are in a separate file (`yt-oauth-token.json`). |
| T-05-03-03 | E (Elevation of Privilege) | `setup-channel-branding.py` CLI | mitigate | Script requires existing OAuth token and valid channel — no elevation risk from CLI args alone |
</threat_model>

<verification>
1. `python3 -c "import json; s=json.load(open('schemas/channel-config.schema.json')); assert 'branding' in s['properties']; assert 'defaults' in s['properties']; print('Schema has branding and defaults')"`
2. `python3 -c "import ast; tree=ast.parse(open('scripts/setup-channel-branding.py').read()); assert any(isinstance(n, ast.ImportFrom) and n.module and 'validation' in n.module for n in ast.walk(tree)); print('Validation import found')"`
3. `grep -q 'manual' .agents/commands/viral-onboard.md && grep -q 'default-privacy' .agents/commands/viral-onboard.md && echo 'viral-onboard.md updated'"`
</verification>

<success_criteria>
1. `schemas/channel-config.schema.json` has `branding` and `defaults` as optional object properties with correct field types
2. `scripts/setup-channel-branding.py` imports `validate_or_raise` and calls it before `save_channel_config()`
3. `.agents/commands/viral-onboard.md` documents the manual avatar limitation and shows the full CLI usage with default settings flags
4. All existing functionality (branding text, banner, watermark, default settings persistence) is unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/05-channel-onboarding-branding/05-03-SUMMARY.md`
</output>
