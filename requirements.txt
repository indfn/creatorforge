# Core dependencies
flask>=2.0.0
requests>=2.25.0
yt-dlp>=2023.0.0

# Optional: Competitor recon (scraping + transcription)
# instaloader>=4.10        # Instagram scraping — pip3 install instaloader
# openai-whisper>=20231117 # Local transcription fallback — pip3 install openai-whisper (requires ffmpeg)

# PDF generation
reportlab>=4.0.0

# YouTube Analytics API (OAuth)
google-api-python-client>=2.0.0
google-auth-oauthlib>=1.0.0
google-auth>=2.0.0

# Environment variable loading
python-dotenv>=1.0.0

# Audio/Visual production
# faster-whisper>=1.0.0    # Force alignment — pip3 install faster-whisper
# Pillow>=10.0.0           # Fallback image generation
# matplotlib>=3.7.0        # Chart generation

# Pipeline infrastructure
jsonschema>=4.20.0
portalocker>=2.8.2
